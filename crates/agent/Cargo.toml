[package]
name = "codesmith-agent"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Model/provider registry, LLM client trait, and wire models for DeepSeek workspace architecture"

[dependencies]
anyhow.workspace = true
codesmith-config = { path = "../config", version = "0.8.48" }
# §E — leaf tool value-types (ToolResult/ToolError/ToolCapability/
# ApprovalRequirement). Cycle-free: tools -> protocol -> {serde, serde_json}.
codesmith-tools = { path = "../tools", version = "0.8.48" }
futures-util = "0.3.31"
reqwest = { workspace = true }
serde.workspace = true
serde_json = { workspace = true }
tokio = { workspace = true }
uuid.workspace = true
