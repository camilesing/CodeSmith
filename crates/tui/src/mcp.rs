//! MCP (Model Context Protocol) runtime.
//!
//! Re-exported from `codesmith-agent-runtime` so that the TUI retains its
//! historical `crate::mcp` path. The implementation lives in the runtime
//! crate; see [`codesmith_agent_runtime::mcp`].
pub use codesmith_agent_runtime::mcp::*;
