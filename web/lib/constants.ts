/**
 * Shared repo literals — keep GitHub references to the main CodeSmith repo in
 * one place so URL construction can't drift (e.g. the wrong-owner link strays
 * this file exists to prevent).
 */
export const GITHUB_REPO = "camilesing/CodeSmith";
export const GITHUB_REPO_URL = `https://github.com/${GITHUB_REPO}`;
